# Dev Setup - Ralph Loop

A minimal environment for the Pi agent to run in an autonomous loop.

## Usage
- Run a single iteration: `./scripts/ralph-once.sh`
- Run the full loop: `./scripts/ralph.sh`

## Structure
- `scripts/`: Loop and utility scripts.
- `specs/`: Project specifications.
- `tasks/`: Backlog and progress tracking.
